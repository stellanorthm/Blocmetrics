<h1><%= @registered_application.name %></h1>

 <div class="row">
   <div class="col-md-8">
     <p><%= @registered_application.url %></p>
     <p><%= pie_chart @registered_application.events.group(:name).count %></p>
   </div>
   <div class="col-md-4">
     <p><%= line_chart @registered_application.events.group_by_day(:created_at).count %></p>
     <%= link_to "Delete Application", @registered_application, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this application?'} %>
   </div>
